import styled from 'styled-components'

export const CanvasSliderSty = styled.div`
  position: absolute;
  top: 0px;
  left: 0px;
  height: calc(100%);
  width: 100%;
  image-rendering: -moz-crisp-edges;
  image-rendering: -webkit-crisp-edges;
  image-rendering: pixelated;
  image-rendering: crisp-edges;
  overflow-y: auto;
  // z-index: 0;
  // background: #ccc;
`